package com.vasiliyrogozhin85.upospa;
import android.app.*;import android.os.*;import android.view.*;import android.widget.*;
public class SettingsActivity extends Activity{public void onCreate(Bundle b){super.onCreate(b);UiKit.screen(this);setContentView(ui());}private View ui(){LinearLayout r=new LinearLayout(this);r.setOrientation(LinearLayout.VERTICAL);r.setPadding(UiKit.dp(this,16),UiKit.dp(this,16),UiKit.dp(this,16),UiKit.dp(this,24));r.setBackgroundColor(UiKit.BG);TextView h=UiKit.tv(this,"‹  Настройки",25,UiKit.TEXT,true);h.setOnClickListener(v->finish());r.addView(h);r.addView(UiKit.card(this,UiKit.tv(this,"uPOSPa v0.8 beta\nРежим безопасности: строгий\nОпасные операции требуют отдельного подтверждения.\nАвтоматическая запись ZIP/payload отключена до полной проверки совместимости.",14,UiKit.MUTED,false)),UiKit.top(this,12));r.addView(UiKit.button(this,"Очистить активную сессию",UiKit.ERROR,v->{new SessionStore(this).clear();Toast.makeText(this,"Сессия очищена",Toast.LENGTH_SHORT).show();}));return r;}}
